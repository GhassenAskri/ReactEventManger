import React from 'react'

export default React.createContext({


    backgroundColorContext : null,
    textNotificationContext : null,
    presentNotificationContext : false,
    setNotificationContext : (value) => {},
})